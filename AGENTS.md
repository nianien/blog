# Blog 项目代理执行规范

本规范适用于本仓库的文章、配图、截图、代码与脚本、内容渲染和验证工作。以用户已确认的目标为准，文章应做到主问题清晰、论证成立、行文连贯、事实准确、图文一致和实际页面可读；具体要求按文章类型与本次改动范围适用。

## 1. 执行原则与规则冲突

- 用户要求明确时直接执行；已经确认的定位、大纲、案例和操作授权持续有效，不逐步重复索要确认
- 复杂任务开始前简述当前目标、相关时间线与已确认事实；用户纠正后立即更新理解，不沿用已否定的假设
- 只在存在会实质改变交付的歧义时澄清；等待期间继续不依赖答案的工作
- 分清已确认事实、推测和未验证结论；出现新线索、找到绕行方案、服务恢复都不等于查明根因
- 本规范解决项目文档间的冲突；系统、开发者及用户当前明确指令优先
- 公共规范只维护在本文件；`CLAUDE.md` 与 `.claude/skills/` 只保留加载入口，技能实现统一维护在 `.agents/skills/`
- Skill 中的逐步确认适用于尚未对齐的新任务，不能覆盖用户已经给出的执行授权
- 不因通用审阅模板而擅自扩写范围，不默认启动子代理，不擅自提交、推送或部署
- 修改前检查工作区，保留用户和其他任务的改动；只提交当前授权范围内的文件
- JavaDoc 和普通代码注释的每行末尾不加中文或英文句号

### 规则分层与适用范围

| 层级 | 内容 | 执行方式 |
| --- | --- | --- |
| 硬要求 | 不伪造事实与实测、关键图文一致、资源可解析、授权边界、真实报告验证范围 | 适用时必须满足；未满足不能宣称完成对应验收 |
| 质量目标 | 主线、论证、衔接、自洽、可读性与有依据的延伸判断 | 结合文章类型判断，用具体问题说明差距，不按形式打分 |
| 默认建议 | 标题和摘要长度、标签数量、slug 词数、视觉样式与章节组织习惯 | 服务于表达和阅读；有明确理由可调整，无需为常规取舍额外请求批准 |

后文涉及具体数值的编辑与样式约定，未注明硬要求时均为默认建议。规范、技能、工具发现的样式偏差不能自动升级为内容错误；规则只在当前任务涉及的类型、渠道和文件上适用。

### 从用户目标推进到实际结果

- “修复”“你测啊”“启动预览”“操作一下”是执行要求；在已有授权范围内直接调用工具，不停留在能力说明、计划或“可以继续”
- 始终追踪原始目标：用户要求实际打开页面，就以目标页面真实加载和可见效果为验收对象，不能悄悄改成文件存在、脚本检查或另一个渠道的预览
- 用户追问状态时简短说明已完成、当前障碍和下一步行动，然后继续工作；不因一次状态问答放弃原任务
- 单个入口失败只证明该次操作失败；先区分应用错误、启动参数、产物版本、资源问题和工具限制，不直接扩大为“功能坏了”或“完全无法验证”
- 有已授权且允许执行的下一步就继续；不能反复描述同一障碍、重复道歉或重复模拟测试，替代真正缺失的验收
- 需要增加预览能力时只做解决当前问题的最小改动；保留既有默认行为，新增模式说明使用方式及与原模式的区别

## 2. 项目与文件位置

项目基于 Next.js，以 Markdown 为内容源。文章位于 `src/content/blog/{大类}/{子类}/`，参考材料位于 `material/`，文章图片位于 `public/images/blog/{article-slug}/`。

| 大类 | 子类 | 选择依据 |
| --- | --- | --- |
| engineering | agentic、aigc、algorithm、architecture、data、domain、middleware、practice | 工程实现、代码、架构与技术方案 |
| insights | business、finance、technology | 商业、金融、科学及技术趋势分析 |
| life | digital、growth、reading | 数字生活、成长与阅读 |
| science | science、cognition、complexity | 认知科学与复杂性科学 |

- AI Agent / LLM 工程归入 `engineering/agentic/`，AIGC 视频与短剧工程归入 `engineering/aigc/`
- 不把文章放在项目根目录或 `material/`，不在文章目录内另建 `assets/`
- 新文件先检查同目录命名和引用方式；旧文件改名、改 slug 时检查现有链接
- 搜索优先使用 `rg` / `rg --files`，不要把历史脚本路径当作当前事实

### 工程职责与验证边界

- `src/lib/blog.ts` 读取文件、解析 frontmatter 与 Markdown，并提供分类、标签和文章导航；路由由目录前缀与可选 slug 共同决定
- `src/lib/content-paths.ts` 是网站与微信共用的文章路由和本地图片解析入口，变更需同时验证两条链路
- `src/lib/categories.ts` 是分类的当前来源，目录表用于选题参考，新增分类时核对实际配置
- `SyntaxHighlightedContent.tsx` 在客户端处理代码高亮及历史 Mermaid 内容；新文章配图仍遵循 SVG 约定
- `scripts/wx/publish.ts` 是独立的微信转换链路，不能用网站预览替代其输出验收；`scripts/wx/preview-server.ts` 提供受限的本机网页预览
- `npm run build` 生成标准 Next.js 产物；`npm run build:export` 生成 out；静态 preview 可能读取旧产物
- 网站 type-check 当前排除 scripts，修改脚本需单独执行针对性验证
- 常用命令、发布路径和环境配置见 README.md 与 scripts/README.md，不从过时脚本名推测执行方式

## 3. 文章内容：围绕主问题与读者收益展开

这个工程用于撰写和发布技术 Blog。文章质量首先取决于主问题是否得到回答、读者是否获得可解释或可操作的认识，格式、配图和页面验证服务于这个目标。核心内容标准集中在本节，语言与去 AI 味标准见第 9 节；写作和审阅技能引用这些标准，不另写一套。

### 3.1 明确目的、读者与承诺

写作前在工作记录中明确：写给谁、解决什么问题、读者最终应理解或完成什么、哪些证据或结果支持它。已有对齐结果直接沿用，不要求用户重复确认，也不把这些工作记录原样搬进正文。

- 宣传与项目介绍：突出已实现的能力、设计亮点、业务价值，用代码、真实场景和可复现结果支撑
- 教程与实践：让目标读者理解为什么这样做，并在明确前提下完成操作、识别结果；步骤之间不能依赖未交代的环境或知识
- 原理与架构分析：解释机制、关键选择和选择带来的结果，帮助读者将认识迁移到类似问题
- 评测与选型：围绕明确问题比较，保持版本、条件和指标口径一致，不将特定场景结论泛化
- 排障与复盘：还原问题、关键证据、排除过程、原因与修复结果，不把恢复或绕行等同于根因查明
- 趋势与观点：提出有依据的判断，解释变化如何发生、影响谁、需要哪些条件，而非罗列热点名称

宣传稿不机械添加缺点清单、风险矩阵、替代方案排行或每章末尾的免责声明；但会改变主张成立与否的前提必须准确交代。宣传不能把规划当现状、适配能力当成已实现的热插拔。

### 3.2 从问题与证据形成主线

- 一篇文章应有清楚的主问题和读者收益：观点文形成中心判断，教程交付可完成的任务，排障记录解释证据与原因，原理文章说明机制；主题名或技术栈列表不能代替主线
- 初步观点是待检验的判断，先核验证据再确定论证；保留相关反证，证据不支持时修正或放弃原判断，不只筛选支持材料
- 用户确认的写作目的和价值定位应保持，事实性结论仍以证据为准；必要时说明调整理由，不为维持预设宣传结论而隐去反证
- 标题、摘要和开篇作出同一份承诺；开篇尽早交代具体问题与预期收获，观点文给出核心判断，不用泛行业背景迟迟推后主题
- 主要章节分别推进回答主问题所需的子问题；材料再丰富，与主线关系弱也应删减、移到注释或另文讨论
- 核心机制、关键案例与结果获得主要篇幅；背景只交代读者理解它们所必需的内容，不按资料数量分配篇幅
- 每项“更快、更简单、更灵活”等价值判断都应落实到具体行为：减少什么工作、改变什么操作、支持什么以前难以表达的场景；比较性结论要有比较对象与依据
- 保留作者的选择与判断：说明在已交代的目标和条件下，为何采用该设计；不能用“各有优劣”回避文章应回答的问题
- 升级文章开篇交代前作、名称或版本变化；围绕新增价值组织全文，区分继承能力与本次变化，不写成变更日志
- 区分主题中的业务目标、表示模型、执行机制与使用体验等层次，按实际主题取舍，不摊平成没有层次的功能清单

### 3.3 让论证能够逐步成立

关键判断需要相应的事实或案例，并解释这些证据如何支持判断。这是论证要求，不是要求每段套用“观点—证据—总结”的固定格式。

- 技术深度来自机制解释：关键输入、状态变化、执行过程、输出及其设计理由，按主张需要展开；类名、接口名和代码行数不能代替解释
- 从“有这个实现”推到“有这个能力”，再推到“带来这个收益”时分别核对依据，不跳过中间环节
- 区分因果关系、相关现象与同时发生的变化；性能、复杂度和可维护性的结论不能仅由架构图推导
- 示例用于说明机制时不要冒充普遍证明；局部测试、单一规模或演示用例的结论应保留其适用范围
- 比较前明确比较维度、使用条件与基线；不把被比较方案简化成明显不合理的做法来衬托自己的设计
- 关键推理若依赖读者尚未知道的概念或条件，先补齐必要解释；不能通过术语连用掩盖推理缺口
- 对影响核心判断的反例或疑问给出解释或缩小结论；优先融入相关段落，不机械增加“缺点专章”

### 3.4 结构由读者的问题推动

- 按理解依赖安排章节：读者先需要知道什么，才能理解后面的选择；可以从问题、案例、结果或关键判断切入，不固定采用“背景—原理—实践—展望”
- 标题表达本节要解决的问题或推进的判断；同级章节讨论层次相近，不把业务目标、代码细节和市场判断混在同一层并列
- 每节回答一个主要问题，并为后文提供必要认识；章节之间应有真实的因果、递进、对比或依赖关系
- 段落围绕一个中心意思展开，句子之间有明确的主语和承接；需要跨越多个推理步骤时拆段，不靠连续长句堆满信息
- 转场说明上一节留下了什么问题、下一节为何接着讨论；能自然承接时无需显式过渡，不靠反复写“接下来”“综上所述”制造连贯感
- 代码、图和表出现前让读者知道看什么，出现后解释关键观察如何支撑正文；不要贴完材料就切换话题
- 贯穿案例随论证推进逐步补充条件和细节；更换案例时说明它承担的解释任务，不让读者反复重建背景
- 结尾回到开篇问题，给出经过论证后的判断、可迁移的认识或具体行动；已有结论说清时可以直接收束，不逐章复述，也不另起未经论证的大主题

### 3.5 自洽性覆盖全文和图文

- 同一术语、对象、缩写和分类维度保持稳定；确需换层次或含义时明确交代，不能在推理中偷换概念
- 版本、时间、场景、参数、单位和统计口径保持一致；同一案例的初始条件、过程和最终结果要能连起来
- 文字、代码、JSON、图注、截图和结果表达同一件事；示意简化不得改变关键语义
- 前文的前提在后文仍然有效；例如前文限定“单次执行内复用”，后文不能直接写成“全局共享缓存”
- 前后结论不能相互抵消；“完全解耦”“无需修改”“任意扩展”等绝对表述必须与实际依赖和范围一致
- 事实、推测、计划和建议使用对应语气；不能在文章后半段把前面提出的设想当作已验证成果
- 用陌生读者视角检查省略：能否明确“它”“该层”“这个结果”指什么，是否需要借助对话历史才读得懂

### 3.6 前瞻性来自有依据的推导

前瞻性可以体现在对设计演进、适用规模、成本变化和能力边界的判断中，不等于每篇文章都加未来预测。与主题无关时不强行扩展到行业或时代层面。

- 从已经观察到的技术或需求变化出发，说明其作用机制，再推导可能带来的工程影响；不把热门技术名称并列当作趋势分析
- 将具体实现放回更大的问题中：这个设计应对什么长期变化，哪些部分稳定，哪些部分可能随着条件变化需要调整
- 明确区分已实现、正在推进、有依据的预测和探索性设想；没有证据时不写确定时间表、普及率或必然替代关系
- 提出方向时说明成立条件、受影响对象，以及可以观察或验证的信号；不靠“未来将更加智能化”收尾
- 趋势、版本能力、市场数据等有时效性的主张需核验来源与时间；不能用过期资料描述当前状态
- 从局部案例推广到普遍判断时补足证据或明确缩小范围；前瞻判断应从正文生长出来，而非突然拔高文章立意

### 3.7 内容发布前自检

先修正观点、论证和结构，再润色句子；语言流畅不能弥补事实错误或论证断裂。以下问题用于编辑自检，不要求原样展示给读者，也不机械打分：

| 检查 | 判断问题 |
| --- | --- |
| 主线 | 读完开篇能否说清本文要解决的问题与预期收获？观点文的中心判断是否明确？正文是否兑现标题承诺？ |
| 论证 | 每个关键结论凭什么成立？事实到结论之间是否缺少解释？ |
| 结构与衔接 | 各节为何按这个顺序出现？是否反复解释同一件事或突然转题？ |
| 自洽 | 术语、条件、代码、配图和结果是否一致？结论范围是否悄悄扩大？ |
| 前瞻 | 延伸判断是否从已知事实推导？条件、信号与不确定性是否清楚？ |
| 表达 | 每段是否带来新信息？是否用抽象赞词代替了具体解释？ |
| 读者收益 | 读者最终多理解了什么，能据此做出什么判断或行动？ |

主线偏离、核心论证断裂、图文冲突和无依据的确定性结论应先修正；文风偏好和是否独立设置某个章节不作为一票否决项。

## 4. 事实与代码核验

关键技术结论必须能追溯到用户提供的事实、参考工程、测试结果或可靠资料。

1. 通读指定材料，查看相关代码入口、调用链、数据模型和已有测试，确认当前版本
2. 按文章实际主张核对相关数据模型、输入输出、状态变化、控制流程和依赖边界，不机械检查与主题无关的实现细节
3. 文档与代码不一致时查明适用版本；不能把不同版本混成同一实现
4. 将核验依据留在工作记录中：文件/符号、版本或提交、测试输入与输出、截图场景；正文按读者需要取舍，不机械插入审计记录
5. 核验否定初步判断时修正或放弃该判断；无法验证的主张应删除、缩小到已知范围或明确标为设想；不得为 SEO 的断言句要求抹去不确定性

具体要求：

- 类名相似、图形相似、目录隔离都不能单独证明行为相同或完全解耦
- 示例代码与 JSON 要符合实际接口及语法；伪代码必须明确标示，不伪装成可直接执行代码
- 性能结论记录相关语言、运行时和组件版本、硬件、工作负载、预热、并发、指标与结果来源
- 只有压测代码或“做过压测”的事实时，可以介绍实验；不得编造吞吐、延迟、倍数或胜出结论
- 将用户提供的数据与自己实测的数据区分开，不声称未执行过的实验已经复现
- 不用真实截图为另一个未运行的配置背书；检查对象标识、输入参数、版本、路径和图注是否对应

### 正文中的来源与证据

- 外部研究、统计数据、关键技术结论和直接引文在相关段落就近标注可追溯来源；优先原始论文、官方文档或原始数据，注明影响结论的版本、日期与条件
- 工作记录中的核验依据不能代替读者需要的出处；常识性说明和纯粹的作者分析不必逐句引用，但分析依赖的外部事实应有来源
- 作者实测交代必要的方法、环境、输入和观察结果；未实际执行的示例标为说明性示例，不套用实测语气
- 外部图片、改绘和代码引用注明来源及必要的使用条件；不把他人成果或经历写成作者原创
- 只引用实际查阅过、直接支持相应主张的材料；不能给错误结论配一个看似权威的链接，也不能编造出处
- 原始资料不可公开时不暴露内部地址或敏感内容，使用获准公开的说明；证据不足以支撑公开断言时缩小结论

规则引擎主题的专项核验见 [.agents/skills/write-article/references/rule-engine.md](.agents/skills/write-article/references/rule-engine.md)，仅在正文涉及对应主张时读取；其他主题不要求 UI AST、规则节点或特定运行时检查。

## 5. 场景、数据库与截图

需要案例说明时，优先使用一个或一组相关场景；纯概念说明不强制构造演示库。以下数据库与界面要求仅在任务涉及相应数据或截图时适用。示意图可以简化，但名称、条件和执行含义必须一致。

- 拍摄前为每张图确定证明目标、场景标识、版本、操作状态和可见信息
- 演示用例覆盖本次主张所需的关键行为，复杂度由证明目标决定，不为展示功能数量堆砌配置
- 若库中缺少用例，在已授权的演示数据范围内构造最小用例并实际运行；不能伪造截图或宣称现有数据已经覆盖
- 新增、修改、覆盖或发布数据以用户授权范围为准；未获授权时可以先准备独立 fixture，说明与截图场景的关系
- 用例应可复现：记录数据库路径、命名空间、场景 ID、版本、数据依赖、输入和预期结果
- 临时数据库用于验证时须明确标记；需要交付的用例应保存到约定项目数据或可重放 fixture，不能只留在临时目录
- 将演示数据合入项目库前先备份并核对依赖，只变更目标场景；不能用整库覆盖抹掉已有数据
- SQLite 使用备份 API、应用导出或一致性快照处理，不能在 WAL 活跃时只复制主文件就宣称备份完整
- “本地已保存”“Git 已跟踪”“已提交”“远端已包含”分别核实，不相互替代
- 截图来自实际运行的界面；不得用生成图片或重画界面伪装成产品运行结果
- 模拟动作明确说明其演示作用，不能描述成真实检索、支付、持久化等业务结果
- 正文中的扩展示例与数据库实际场景要明确区分，不能默认新增示例已入库
- 裁剪和组合可以改善阅读，但不得隐藏改变结论的信息；组合图应说明关系，保留可读的原图
- 检查弹窗、遮罩、滚动位置、选中状态和布局稳定性后再截图；不得通过篡改页面数据制造结果

## 6. Frontmatter 与 Markdown

每篇文章以可解析的 YAML frontmatter 开头。新文章按下表填写；长度和数量是编辑建议，不作为凑字数、强制改标题或重写旧文的依据：

| 字段 | 约定 |
| --- | --- |
| title | 准确表达主题与承诺，建议不超过 30 个中文字；不为塞关键词破坏表达 |
| description | 建议 120–160 字，尽早给出主题和预期收获；准确清楚优先于字数 |
| pubDate | 使用约定发布日期，未指定时新文章默认当天；修改既有文章不擅自重置 |
| tags | 建议 3–6 个相关标签，不为凑数添加弱相关标签 |
| slug | 新文章使用简洁的英文 kebab-case，同目录唯一；3–6 个词仅作建议，既有路由保持兼容 |
| author | 固定为 skyfalling |

- 使用实际 YAML 解析器检查 frontmatter；字符串内部优先用中文引号或书名号，避免未转义的直引号破坏定界
- 旧文章缺少 slug 时按文件名回退，链接需按 `src/lib/content-paths.ts` 的共享路由规则生成并验证
- 正文不写一级标题，页面会渲染 frontmatter 标题
- 开篇尽早说明主问题和预期收获；篇幅由必要背景决定，不为前 100 字之类的指标生硬塞入结论
- 每个 H2 明确该节要解决的问题或推进的观点，不强制使用断言句、加粗金句或固定总结
- 加粗只强调关键判断，括号、引号和书名号优先移到加粗外；正则只是候选检查，最终以页面渲染为准
- 结构化对比使用 Markdown 表格，表格不得伪装为 SVG
- 代码块用于代码、配置、DSL、Prompt 模板或明确的伪代码，选择正确语言标签
- 普通列表与步骤用 Markdown 列表，需要对照维度时再用表格；不把普通文字塞进代码块
- 架构图、流程图、树形图使用 SVG，不用 ASCII art
- 图片 alt 描述图中关系或结论，不使用“image”“截图”等空名称
- 图片 Markdown 使用 `/images/blog/{article-slug}/{filename}`，不要把本机绝对路径写进发布正文

## 7. SVG：先确认图意，再计算布局

### 7.1 语义检查

绘图前先确定图要回答的问题、节点、边、方向、层次及图例。必要时在工作记录中写出节点与边的清单。

- 执行方向、数据流、依赖关系和树的包含关系要明确区分，不能用相同箭头混淆含义
- 与代码逐项核对分支、汇合、短路、循环入口/退出、轮次条件和结果传递
- 有回边的有向图不能称为 DAG；将循环封装为单节点时说明图的抽象层次
- 同一逻辑的两种表示应保持语义等价，不能在对比图中悄悄增加等待或改变执行次数
- 两种模型或表示之间可能有合并、展开与映射，图形相似不能证明结构完全相同
- 图中的计数和汇总标明统计对象、层级与口径，不能跨层重复计数
- 箭头、标签、图注及相邻正文必须表达同一事实

### 7.2 文件与样式

SVG 放在 `public/images/blog/{article-slug}/`，用语义明确的英文文件名。

- 根节点必须提供有效 `viewBox` 以适配正文缩放；默认字体建议为 `system-ui, -apple-system, 'PingFang SC', sans-serif`，有明确字体设计时可调整
- 代码类文字可用 `'SF Mono', 'Menlo', monospace`
- 圆角矩形默认建议 `rx="6"` 或 `rx="8"`，描边默认建议 `stroke-width="1.5"`；按图形比例调整时以实际显示为准，不强制覆盖已有样式
- 使用浅色填充、深色描边，保持语义配色一致；判断结果不能只靠颜色区分
- 新图默认使用 `<polygon>` 箭头和内联样式，便于核对及转换；已有 marker 或 CSS 不自动判为错误，需检查实际箭头、继承样式与目标渠道显示，不能为消除提醒删掉语义
- 水平、垂直连接优先，转折使用正交折线；避免穿过文字和无关节点
- 连线先画、节点后画、文字最后画，减少线条覆盖文字

### 7.3 几何与可读性

- 先规划画布、分区、网格、节点尺寸和端口位置，再确定文字和连线坐标
- 用浏览器实际字体渲染后的边界核对文本；仅检查 text 的 x/y 坐标不足以判断文字是否溢出
- 矩形宽高覆盖全部文字及内边距；长标签换行或扩容，不压缩字体硬塞
- 为节点、箭头、标签与画布边缘保留明确空隙；留白用于分组，避免主体过小而大面积空白
- 箭头尖端对准目标端口，三角形方向与最后一段连线的方向一致；线段与箭头衔接，不能悬空、倒向或穿入文字
- 树的层次、并行分支、循环范围应一眼可辨；复杂图优先拆分，不能靠无限加宽解决
- 在正文实际显示宽度下检查字号和对比度，不能以放大后的原图清晰代替正文可读
- 手机端至少能看清主结构与主要标签；详细信息可通过原图放大查看，但放大入口不能补救主图不可理解

## 8. 按任务范围完成验证

先明确本次验收的目标、入口和预期结果，再按下表选择范围。“脚本通过”“图片解码成功”“返回 200”不能代替已要求的真实操作与视觉验收；用户明确要求实际打开或浏览器复测时，直接完成对应操作。

| 本次工作 | 必要检查范围 |
| --- | --- |
| 仅规范或文档说明修改 | 规则一致性、引用与差异，不运行无关构建或浏览器验收 |
| 纯标点、错字等局部修改，无新增事实、链接或布局变化 | 核对修改段落及差异，不自动重跑全篇、多视口和多渠道验收 |
| 事实、机制、参数或代码示例变化 | 核对相关证据和案例；可执行结果属于主张时补充相应运行验证 |
| 新文章、结构重排、表格、配图或可见排版变化 | 验证目标渠道的相关区域和桌面、窄屏效果；只复测受影响区域 |
| 代码、路由、渲染或发布链路变化 | 相关类型检查与有区分度的回归；涉及构建或路由时补充构建验证 |

下列阶段仅检查本次适用的内容，无配图时不强制图意审计：

| 阶段 | 必须确认 | 未通过时 |
| --- | --- | --- |
| 内容核验 | 核心主张、代码、场景、数据与实际版本一致 | 修正主张或补充可验证证据 |
| 图意核验 | 节点与连线语义、执行顺序、图注一致 | 重画相关结构 |
| 文件检查 | YAML、Markdown 与资源可解析；SVG 错误修复、提醒按实际图形核对 | 修复确定错误，不为消除提醒机械改图 |
| 页面实测 | 文章真实路由中图文可读，链接和放大入口可用 | 调整正文、图片或必要的局部样式 |
| 修正复测 | 重新检查修改区域及受影响页面 | 未达标继续调整，不只报告问题 |

### 8.1 命令检查

在项目根目录按需运行：

```bash
python3 .agents/skills/svg-check/svg-audit.py <article-slug>
git diff --check
```

- SVG 脚本接受图片目录名、实际目录或单个 SVG 路径；全库只读审计显式传 `--all`，自动修复必须指定目标
- 确认扫描数量覆盖本次相关 SVG 且不为零；目录未命中或零扫描不能算通过
- SVG 审计将解析和必要属性错误与样式、可疑几何提醒分开；错误使命令失败，提醒需结合图形判断，退出码 0 不代表视觉验收完成
- 按本节范围表选择检查，修改资源引用时确认目标存在，不机械扩大到无关文件或渠道
- 修改 TypeScript、组件或渲染逻辑时运行 `npm run type-check`；涉及构建、路由或发布配置时补充相关构建验证
- 一次运行多个检查时逐项核对退出码与输出，必要时使用失败即停；不能把最后一条命令成功当成前面测试通过
- 新增文件也要检查，不能只依赖未包含它们的 `git diff`
- 检查结束后复核工作区，不把缓存、临时截图、数据库旁文件或其他任务的改动混入提交

### 8.2 浏览器实测

对本次需要页面验收的修改，使用浏览器访问目标渠道的真实页面：

- 确认加载的是正确工程、当前版本和真实文章路由；已启动的预览优先复用
- 检查桌面与窄屏，例如 1280px 和 390px；触及移动端布局时增加 320px 边界检查
- 页面不得整体横向溢出；宽表格和代码块可在各自容器内横向滚动
- 检查新增/修改图片均加载成功，正文缩放下可读，标题、图注、表格和代码无裁切
- 滚动到本次改动区域实际查看截图，不能只读取 DOM 或图片 naturalWidth
- 对修改过的 SVG 同时检查原图和嵌入正文的效果，核对箭头、文字、重叠、留白与图例
- 验证新增链接、图片放大和交互；截图中的配置与实际运行数据要对应
- 每次视觉修正后复测受影响区域；已有检查通过后不无理由重复全量检查
- 核对预期图片数与成功加载数，区分懒加载尚未触发和加载失败；记录实际检查的路由、视口和区域
- 用户要求实际打开时，必须确认目标页面确已出现在浏览器中；发出 open 调用、返回进程 ID 或模拟调用成功均不足以证明打开成功
- 检查完成后提供可访问的预览地址；为用户查看而启动的服务保持可用，说明停止方式，不结束其他任务的服务

### 8.3 网站与微信分别验收

只验证用户要求或本次修改实际影响的渠道。未指定渠道的新文章默认按网站验收，不自动增加微信任务；共享路由、图片解析等会影响两条链路的改动需分别回归。

| 目标 | 验证入口 | 能证明的范围 |
| --- | --- | --- |
| 网站开发页面 | 当前工程的真实文章 HTTP 路由 | 当前代码的网站渲染和交互 |
| 网站静态输出 | 本次构建产物的 HTTP 预览 | 静态导出后的路由与资源 |
| 微信本地 HTML | `./scripts/cli.sh wx:preview <md>` | 文件生成；默认自动打开仍需实际观察 |
| 微信网页预览 | `./scripts/cli.sh wx:preview <md> --serve` | 同一微信转换链路生成的网页和配图 |
| 微信客户端最终效果 | 经用户授权后的微信端检查 | 微信客户端实际显示，不能用本机网页结果代替 |

- 微信网页预览读取终端实际输出的地址，不猜测或固定端口；查看正文、JPG/PNG 以及 SVG 转换结果
- `--no-open` 明确关闭自动打开；带此参数的运行不能证明默认自动打开成功。只有默认自动打开属于本次目标时才补测该入口，网页预览验收不附带要求验证所有打开方式
- 文件生成、资源解码、HTTP 响应、浏览器加载、页面视觉效果分别记录；测试使用 stub 或 mock 时明确标记，不计作真实浏览器验收
- 新增网页预览并验证成功，只证明新入口可用；不能据此声称原来的 file URL 或系统打开方式已经恢复
- 预览不得上传素材、创建草稿或发布；真实发布接口需要对应授权，不能为测试排版顺带调用
- 本机预览只监听回环地址，只提供本次生成页面和选定资源；不开放工程目录、环境文件或任意文件读取，按需要限制脚本与外部资源

### 8.4 工具受限时继续诊断与执行

1. 记录具体失败操作、错误信息和限制来源，确认失败发生在哪一步；不要仅凭“打不开”推断原因
2. 检查参数、服务状态、实际地址、产物时间和资源路径，优先使用能区分不同原因的检查；不重复没有新信息的尝试
3. 评估是否存在规则允许、风险实质降低且仍满足目标的替代方式；技术上可行不等于获准执行
4. 明确禁止替代访问或绕过时，不通过换工具、换协议、代理地址或系统命令达到被禁止的访问结果，也不要求用户关闭保护；受限 HTTP 预览不是自动获准的通行证
5. 替代方式被允许时直接落实并做真实验收，说明其验证范围；不得将替代方案可用写成原故障根因已查明
6. 确实没有允许的路径时，完成不受影响的工作，一次清楚说明剩余未验证项、证据和所需外部动作；不夸大为整个项目无法验证，不用重复解释代替进展

### 8.5 本地服务与验证环境

- 优先复用已确认属于当前工程、对应模式的服务；同时核对工作目录与启动命令，不能只凭端口或进程名认定归属
- 不使用全局 pkill、killall 或按端口无差别结束进程；遇其他服务占用时保留原服务，使用允许的空闲端口或明确报告冲突
- 区分 next dev、next start 和静态预览；不能用旧构建或其他服务的页面证明当前修改有效
- 构建会影响正在使用的预览时，优先在隔离目录验证，避免覆盖其缓存或产物
- 检查通过后只对新增修改、失败项和未决问题补测；规范修改不重复运行无关功能测试

## 9. 行文与去 AI 味

目标是让读者感受到清楚的判断、具体的解释和自然的叙述。去 AI 味依靠内容取舍与表达质量，不依赖禁词数量、检测分数或人为制造不规整。

### 9.1 清楚、顺畅、有信息

- 中文为主，优先使用熟悉的词、明确的主语和具体动词；保留必要术语，不用口语替换精确概念
- 小众概念首次出现时在语境中解释；熟悉的术语不机械补百科定义，不让解释打断当前问题
- 句子长短由信息关系决定；并列条件过多时拆开，连续短句显得生硬时合并，不强制每句或每段字数
- 指代明确，时间、因果、转折和条件关系准确；少用没有所指的“这一能力”“这种范式”“上述机制”
- 让重要结论容易找到，其他内容承担解释或证明作用；不把每句话都加粗，不连续堆叠看似有力的断言
- 类比只帮助理解特定关系，并保持映射稳定；类比不能代替技术定义或证明，熟悉的机制无需再套一层比喻

### 9.2 消除空泛和机械表达

- 删除与问题无关的时代背景、泛行业铺垫和“本文将从以下几个方面”的目录复述
- 把“强大、灵活、高效、赋能、闭环”等抽象评语落实为具体变化；找不到事实支撑时删除，不只是换一个近义词
- 不反复使用“这不是 X，而是 Y”、整齐排比、对仗标题或每节三点式；只有真实的对比或并列关系才使用相应结构
- 不要求每章都有金句、总结、局限和展望；避免段落长度、标题句式和结尾模式过度齐整
- 删除换措辞重说一遍的段落、反复提醒读者的导读和没有新增认识的总结
- 不用“范式跃迁”“第一性”“元命题”等大词替代具体论证；确需使用时解释它在本文中对应的判断
- 少用不承担逻辑作用的“值得注意的是”“显而易见”“毋庸置疑”等引导语；是否保留取决于语义，而非词频配额
- 不自我评价“深刻、全面、严谨”，不写“诚实地说”“必须承认”等自辩旁白；让证据和表达承担说服作用
- 不靠故意加入错别字、随机口语、网络俚语或破坏结构来伪装人工写作
- 没有作者提供的经历时，不编造“我们线上遇到”“实测发现”“踩坑多年”等第一手经验；作者语气应来自明确判断而非虚构身份

### 9.3 用具体表达替换空话

以下示例只演示表达方式，具体机制与数值必须由当前文章的材料证明，不能直接套用为事实。

| 空泛表达 | 有依据时可采用的表达 |
| --- | --- |
| 系统具有强大的扩展能力 | 新增规则通过注册入口接入，既有流程仍按规则 ID 引用 |
| 参数化为业务赋能 | 两个策略节点共用同一条判断逻辑，分别配置自己的阈值 |
| 在技术飞速发展的今天，这一问题日益重要 | 同一条规则被多个流程引用后，修改阈值会影响哪些节点，成为配置管理需要解决的问题 |
| 未来系统将更加智能化 | 若规则配置持续增长，可以进一步检查配置冲突；是否引入自动检查，取决于冲突频率与维护成本 |

### 9.4 编辑与审阅的边界

- 润色先消除歧义、冗余和衔接断裂，再调整语气；不能为了句式漂亮改变技术含义、删掉必要前提或扩张结论
- 通读时同时检查读者能否跟上，以及每段是否增加事实、解释、判断或必要承接；不能只扫描词频和格式
- 文章已有鲜明且合适的作者风格时保留，不把不同主题统一改成同一种模板腔
- 审阅记录与对外正文分开；宣传文章重在价值表达真实清晰，不因没有缺点专章、固定结尾或独立展望而扣分
- 指出具体句段、问题与改法；“有 AI 味”“不够高级”不能单独构成审阅意见

## 10. Skills 与交付

| 工作 | 使用的本地 Skill |
| --- | --- |
| 撰写或重构文章 | `.agents/skills/write-article/SKILL.md` |
| 内容审阅 | `.agents/skills/review-article/SKILL.md` |
| SVG 创建、修改与检查 | `.agents/skills/svg-check/SKILL.md` |

读取适用 Skill，首次使用时说明；按本文件和用户已确认的目标解释其通用流程。

交付以实际结果为主：说明改了什么、通过哪种入口验证、用户在哪里查看，以及仍影响结论的未完成项。具体测试数量、图片数量和“已打开”等表述必须与记录一致。

- “已生成”“已启动”“已打开”“已验证”“已提交”“已推送”分别提供对应证据，不互相替代
- 成功的替代验证与原路径的状态分别说明，不省略 mock、不同渠道或不同运行模式等关键条件
- 没有新进展时不连续发送重复的限制说明；有结果时直接报告结果，不以长篇自辩或过程回顾掩盖未完成事项

用户要求提交或推送时，确认仓库、分支和文件范围，提交后检查结果，推送后确认远端包含该提交；博客仓库、参考代码仓库和演示数据库的状态分别说明。
